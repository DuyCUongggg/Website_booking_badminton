# Web khách hàng — đặt sân cầu lông

Giao diện tĩnh (HTML/CSS/JS). Chạy demo trên trình duyệt, không cần backend (`js/core/config.js`: `DEMO_MODE = true`).

**Deploy GitHub Pages:** đẩy toàn bộ thư mục này (các file `.html`, `js/`, `assets/`).
